// Ce programme permet de structurer les fonctions dependantes de l'executable note fiche

#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>

// definition des macros

// les prototypes
void new_fichier(void);

// definition et corps des fonctions
int incremant = 0;
char *note = "note";
void new_fichier(void){
    
    /*FILE *fichier;
    strcat(note,(char)i);
    strcat(note,".txt");
    fichier = fopen(note,"w");
    fclose(fichier);
    */
}